CREATE DATABASE CHEESECAKE;
USE CHEESECAKE;
CREATE TABLE ORDERS{
ORDERID int,
MONTH CHAR(3),
DAY int,
QUANTITY int,
TOPPING VARCHAR(12),
NOTES VARCHAR(512)
};
ALTER TABLE ORDERS
ADD CONSTRAINT checkVals CHECK (TOPPING='Cherry' OR TOPPING='Plain' OR TOPPING='Chocolate');
DESCRIBE ORDERS;
SELECT * FROM ORDERS;
SELECT * FROM ORDERS 
WHERE MONTH='Feb';
INSERT INTO ORDERS (
	ORDERID,
	MONTH,
	DAY,
	QUANTITY,
    TOPPING,
    NOTES
    )
VALUES (
    10,
    'Sep',
    10,
    2,
    'Cherry',
    "Extra cherries"
);